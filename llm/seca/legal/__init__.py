"""Public legal pages (privacy policy) served as static HTML.

Distinct from the internal working draft in ``docs/PRIVACY_POLICY.md``
(which carries compliance-gap tracking + a claim-to-code evidence map
that must never reach users).  ``privacy_policy.html`` here is the
PUBLISHED text; ``test_privacy_policy_page.py`` guards that it never
contains draft/placeholder/annex markers.
"""

<?xml version="1.0" encoding="utf-8"?>
<!--
    Atrium coach-chat panel background.

    The chat is a NON-MODAL, bottom-docked half-height panel over the
    live game board (see ChatBottomSheet.onStart).  Because it shares
    the screen with the board behind it, it needs to read as a distinct
    raised surface rather than blending into the game's gradient:
      - raised surface fill (atrium_bg_surface, a step above bg_base),
      - rounded top corners so it clearly "docks" over the board,
      - a hairline top/side edge to separate it from the board.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/atrium_bg_surface" />
    <corners
        android:topLeftRadius="20dp"
        android:topRightRadius="20dp" />
    <stroke
        android:width="@dimen/atrium_hairline_thickness"
        android:color="@color/atrium_hairline_strong" />
</shape>

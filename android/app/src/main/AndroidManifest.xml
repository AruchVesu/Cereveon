<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CereveonApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="Cereveon"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Cereveon.Atrium">

        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lichess OAuth redirect trampoline: catches the custom-scheme
             redirect from the system browser and forwards it to
             LoginActivity (see LichessAuthRedirectActivity docs).  Must
             be exported so the browser can deliver the VIEW intent. -->
        <activity
            android:name=".LichessAuthRedirectActivity"
            android:excludeFromRecents="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="lichess-auth"
                    android:scheme="ai.chesscoach.app" />
            </intent-filter>
        </activity>

        <!-- Lichess LINK OAuth redirect (ai.chesscoach.app://lichess-link):
             distinct host from sign-in so a logged-in user's link flow
             completes in its own handler (LichessLinkRedirectActivity),
             never the login screen.  Exported for the browser VIEW intent;
             defended by the persisted-state CSRF check inside. -->
        <activity
            android:name=".LichessLinkRedirectActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/Theme.Cereveon.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="lichess-link"
                    android:scheme="ai.chesscoach.app" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OnboardingWelcomeActivity"
            android:exported="false" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".OnboardingCompleteActivity"
            android:exported="false" />

        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".PaywallActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysHidden|adjustResize"
            android:exported="false" />

    </application>

</manifest>
